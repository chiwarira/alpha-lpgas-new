[build]
builder = "NIXPACKS"
watchPatterns = ["backend/**", "railway.toml"]

[deploy]
startCommand = "if [ -f start.sh ]; then bash start.sh; elif [ -f backend/start.sh ]; then cd backend && bash start.sh; fi"
